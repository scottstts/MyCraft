export function ClickToEnterOverlay() {
  // Pointer-lock loss is represented by PauseMenu now. Keep this component as
  // a no-op export for callers that still import the legacy name.
  return null
}

export default ClickToEnterOverlay
